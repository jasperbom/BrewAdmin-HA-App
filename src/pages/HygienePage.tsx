// HygienePage — de hygiëne checklist is embedded in BatchesPage.
// Dit bestand exporteert de constanten en biedt een placeholder component.
export { DEFAULT_HYGIENE_GROUPS, DEFAULT_HYGIENE_ITEMS } from '../utils/constants'

// HygienePage is niet als zelfstandige pagina aanwezig in de broncode.
// De checklist wordt getoond binnen BatchesPage per batch.
const HygienePage = () => null
export default HygienePage
